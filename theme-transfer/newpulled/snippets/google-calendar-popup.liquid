{% comment %}
  ==============================================================================
  GOOGLE CALENDAR POPUP SNIPPET
  - Renders a modal pop-up for a Google Calendar appointment page.
  - Requires `section_id` to be passed for unique element IDs.
  ==============================================================================
{% endcomment %}

<style>
  .gcal-popup-modal {
    display: none; /* Hidden by default */
    position: fixed;
    z-index: 9999;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    background-color: rgba(0,0,0,0.6);
    -webkit-animation: gcal-fade-in 0.5s;
    animation: gcal-fade-in 0.5s;
  }

  .gcal-popup-modal__container {
    display: flex;
    flex-direction: column;
    margin: 2vh auto;
    width: 95%;
    max-width: 980px;
    height: 94vh;
    border-radius: 24px;
    overflow: hidden;
    position: relative;
    background-color: #fff;
    box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2),0 6px 20px 0 rgba(0,0,0,0.19);
  }

  .gcal-popup-modal__header {
    flex-shrink: 0;
    text-align: right;
    padding: 12px 16px;
  }

  .gcal-popup-modal__close {
    color: #5f6368;
    background: transparent;
    border-radius: 50%;
    font-size: 28px;
    line-height: 28px;
    width: 32px;
    height: 32px;
    text-align: center;
    font-weight: normal;
    cursor: pointer;
    display: inline-block;
    border: none;
    transition: background-color 0.2s;
  }
   .gcal-popup-modal__close:hover {
    background-color: #f1f3f4;
   }

  .gcal-popup-modal__content {
    flex-grow: 1;
    overflow-y: auto; /* Allow content to scroll if needed */
  }

  .gcal-popup-modal__iframe {
    width: 100%;
    height: 100%;
    border: none;
  }
  
  .gcal-popup-modal__footer {
    padding: 16px;
    text-align: center;
    font-size: 12px;
    color: #70757a;
    background-color: #f8f9fa;
    border-top: 1px solid #dadce0;
    flex-shrink: 0;
  }

  .gcal-popup-modal__footer a {
    color: inherit;
    text-decoration: underline;
  }

  @-webkit-keyframes gcal-fade-in {
    from {opacity: 0}
    to {opacity: 1}
  }

  @keyframes gcal-fade-in {
    from {opacity: 0}
    to {opacity: 1}
  }
</style>

<div id="gcal-popup-{{ section_id }}" class="gcal-popup-modal" aria-modal="true" role="dialog">
  <div class="gcal-popup-modal__container">
    
    <div class="gcal-popup-modal__header">
      <button class="gcal-popup-modal__close" data-gcal-popup-close>&times;</button>
    </div>

    <div class="gcal-popup-modal__content">
      <iframe class="gcal-popup-modal__iframe" src="" data-gcal-popup-iframe title="Appointment Scheduling"></iframe>
    </div>
    
    <div class="gcal-popup-modal__footer">
      Powered by <a href="https://calendar.google.com/calendar/appointments" target="_blank" rel="noopener">Google Calendar appointment scheduling</a>.
    </div>

  </div>
</div>